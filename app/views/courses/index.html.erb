<h1>Courses</h1>
<% @courses.each do |course|%>
    <p> course name: <%= course.name%> </p>
    <p> <%=link_to "show this course", course_path(:id=>course.id)%>
<% end %>

<%= link_to "add a new course", new_course_path %> <br> <br>
<%= link_to "add a quarter to filter by", new_active_quarter_path %> <br><br>
<%= link_to "show all courses", delete_filter_path %>

